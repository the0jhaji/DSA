#include <iostream>
#include <vector>
using namespace std;
int main()
{
//     vector<vector<int>> arr(5, vector<int>(5, 1));
//     //2d array created with 5 rows 4 cols with initial value of 1.
//     int totalRows=arr.size();
//     int totalCol=arr[0].size();
    vector<vector<int>> brr(4);
    brr[0]=vector<int>(4);
    brr[1] = vector<int>(2);
    brr[2] = vector<int>(3);
    brr[3] = vector<int>(5);
    int totalRows=brr.size();   
    
}